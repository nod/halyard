#!/bin/sh


if ! command -v entr &> /dev/null
then
    echo ERR entr not found 
    exit 1
fi

# run tests whenever files change

while : ; do
    find . -name '*.go' | entr -p -d go test -v ./...
done

