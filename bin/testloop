#!/bin/bash

entr=`which entr`
if [ ! -x $entr ]; then
    echo ERR entr not found
    exit 1
fi

# run tests whenever files change

while : ; do
    find ./ -name '*.go' | entr -d go test -v ./...
done

